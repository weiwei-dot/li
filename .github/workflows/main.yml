
name：MYCI
On:
 Push:
   Branches:
    -master
   Paths:
 -src/*
Jobs:
Jib1:
 Runs-on:ubuntu--latest
Steos:
-uses:actions/checkout@v1
-uses:actions/setup-node@v1
-run:
Npm install
Npm run build--if-present
Npm test
